let trompo = ['toma 1', 'toma 2', 'pon 1', 'pon 2', 'todos ponen', 'toma todo'];
let vueltasTrompo = 5;
let numeroPrueba = Math.random()*5; // da un numero decimal 
let floor = Math.floor(numeroPrueba)// redondea el numero decimal que da Math.random()

// posible valores 0, 1, 2, 3, 4, 5

function juegoDelTrompo (trompo, vueltasTrompo){
    let numeroAleatorio = 0;

}